/**
 * Optional curated educator cards for the dashboard when the platform has few profiles.
 * Empty by default — only registered educators appear in "Top rated".
 */
export const SEED_EDUCATORS = [];
