/**
 * Built-in marketplace catalogue (optional seed). **Ship empty:** do not add demo rows here.
 * All paid/browse listings come from educators (stored in PostgreSQL via server/educatorCourses.js).
 * Row shape if you seed later: id, title, educator, lessons, price, subject, thumb ("", warm, cool, mint, rose, slate, …)
 */
export const CATALOG = [];
